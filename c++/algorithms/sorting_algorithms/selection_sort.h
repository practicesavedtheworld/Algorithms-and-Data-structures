#include <iostream>

namespace SelectionSort
{   /*
     * Selection sort algorithm
     * The idea is to find the minimum element in the array and swap it with the current element
     */
    void selectionSort(int64_t arr[], size_t arr_size);
} // namespace SelectionSort